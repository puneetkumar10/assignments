1. clone both nodes.
2. run npm install in both nodes so that node modules get installed
3. run npm start on both nodes , this will run both node instances.
4. NODE1 -> localhost:3000/api-docs/
5. NODE2 -> localhost:3005/api-docs/